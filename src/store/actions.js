import API from '../api.js';
import Vue from 'vue';
import router from '../router';

const actions = {
    // getArticleList({commit}) {
    //   API.getArticleList()
    //   .then(response => {
    //     let list = response.data.map(element => {
    //       return { number: element.number, id: element.id, title: element.title, articleTime: moment(element.created_at).format('MMM YYYY') };
    //     });
    //   })
    // }
};

export default actions;
